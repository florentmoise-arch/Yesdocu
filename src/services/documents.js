// YesDocu 2.2.0

function listSuivi() {
    // This function will return all columns including motif_rejet, status, source, hotfolder, etc.
    const columns = [
        'motif_rejet',
        'status',
        'source',
        'hotfolder',
        // Add other columns as needed
    ];

    return columns;
}